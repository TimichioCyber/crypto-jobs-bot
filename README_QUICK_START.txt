1) Replace your repo files with these files.
2) Remove old parser files you do not use.
3) Create .env from .env.example
4) Put TELEGRAM_BOT_TOKEN into .env or Railway variables
5) Install:
   pip install -r requirements.txt
6) Run:
   python bot_main.py

This version uses only Greenhouse and is meant as a clean base.
